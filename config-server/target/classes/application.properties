spring.application.name=config-server
server.port=8888

# Config Server Local File System Configuration
spring.cloud.config.server.native.search-locations=file:./configs,file:/app/configs

# Enable native profile to use local file system
spring.profiles.active=native

# Actuator configuration
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always